<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.home.board.mapper.CommentMapper">

	<resultMap type="comment" id="commentInfo">
		<result column="id" property="id" />
		<result column="register_date" property="registerDate" />
		<result column="content" property="content" />
		<result column="user_id" property="userId" />
	</resultMap>

	<select id="getCommentList" parameterType="int"
		resultMap="commentInfo">
		select id, register_date, content, user_id
		from comment
		where article_id = #{number}
	</select>
	
</mapper>